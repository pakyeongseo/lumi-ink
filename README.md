# v63.8 — 아이디어 보드 조절 UI·HTML 입출력

- 포스트잇의 조절 핸들을 캔버스 오버레이로 분리해 잘림을 제거하고, 보드 경계에서는 핸들이 자동으로 안쪽으로 뒤집힙니다.
- 아이디어 보드를 배치·디자인·미디어·캔버스 배경까지 포함한 꾸며진 HTML로 내보내고, 해당 HTML을 다시 편집 가능한 보드로 가져올 수 있습니다.

# v63.7 — 통합 인물 카드 다중 병합

- 선택 모드에서 페르소나(단일 모드)와 캐릭터 모음을 함께 선택해 새 **캐릭터 모음**으로 합칠 수 있습니다.
- 각 원본의 모든 인물 페이지(국문·영문 설정, 태그, 포트레이트, 정사각 썸네일, 갤러리, 크리에이터 노트)와 페이지별 보조 필드를 새 ID로 복제합니다.
- 원본 메모는 삭제하지 않으며, 병합 직전 자동 백업을 남깁니다. 원본 메모 수준의 호환·확장 필드는 결과 데이터의 `mergedSources`에 출처와 함께 보관합니다.
- HTML 작업실과 아이디어 보드는 기존처럼 합치기 대상에서 제외됩니다.

# v63.6 — 현재 기능 기준 매뉴얼 갱신

- `Lumi_Ink_Manual_1.html`을 v63.5 기능 기준으로 전면 갱신했습니다.
- HTML 작업실, 통합 인물 카드, 자유 메모 이미지 크기 조절, 아이디어 보드·템플릿 라이브러리·색상·캔버스 이미지 설정의 사용법과 백업 범위를 반영했습니다.
- 서비스 워커 캐시를 갱신해 설치형 앱에서도 새 매뉴얼이 우선 반영됩니다.

# v63.4 — 아이디어 보드 템플릿 라이브러리·확장 레지스트리

- 보드 배경과 포스트잇 선택 창을 실제 질감이 보이는 **2열 썸네일 라이브러리**로 개편했습니다.
- 현재 선택한 템플릿은 선택 배지·테두리·상태 문구로 즉시 구분됩니다.
- `idea-board-templates.js`에 키·이름·설명만 등록하면 선택 라이브러리에 자동으로 나타납니다.
- `idea-board-custom-templates.css`는 새 배경·포스트잇 CSS만 넣는 전용 확장 슬롯입니다. 앱의 드래그·저장·리사이즈 로직을 건드릴 필요가 없습니다.
- `idea-board-design-guide.md`와 `idea-board-template-registry-guide.md`에 AI 의뢰 프롬프트, 키 규칙, 등록 예시, 검수 항목을 정리했습니다.

# v63.2 — 아이디어 보드 제스처·위치 저장 보정

- 조각 정규화가 객체 참조를 끊지 않도록 수정해, 드래그한 `x/y` 좌표가 보드·아티팩트 전환과 재진입 후에도 유지됩니다.
- 길게 누른 뒤 발생하는 `contextmenu`/클릭을 차단하여 이동 중 옵션 팝업이 열리지 않게 했고, 파일·메모 링크 조각도 길게 눌러 이동할 수 있게 보강했습니다.
- 이동 모드에서는 포스트잇·오디오·영상의 내부 조작부도 드래그 대상으로 전환됩니다.
- 회전 핸들을 0.34초 안에 두 번 탭하면 회전값이 `0°`로 복원됩니다.
- `idea-board-design-guide.md`에 AI용 보드 배경·포스트잇 디자인 제작 명세와 복사 프롬프트를 추가했습니다.

# v63.1 — 아이디어 보드 스튜디오 패치

- 조각의 위치, 크기, 회전, 포스트잇 내용과 스타일을 보드 데이터에 즉시 저장합니다.
- 이미지·GIF·동영상은 원본 비율을 읽어 기본 크기를 잡고, 영상은 `contain` 표시로 잘리지 않습니다.
- 조각을 길게 눌러 이동하고, 더블클릭/우클릭에서 이동·크기·회전·미리보기·삭제 옵션을 엽니다.
- 포스트잇은 형광 마커/테이프 노트 × 5색 컬러 템플릿을 제공합니다.
- 보드 배경은 청사진 그리드와 스크랩북 페이퍼, 두 개의 확장 템플릿을 제공합니다.

# v63 — 아이디어 보드

- 새 `아이디어 보드` 메모 타입: 대형 캔버스, 포스트잇, 이미지/GIF, 오디오, 30MB 이하 영상, 일반 파일, 기존 메모 인용 링크
- 빈 캔버스 길게 누르기 및 상단 추가 메뉴, 조각 길게 눌러 이동, CANVAS / ARTIFACTS 이중 보기
- 보드 미디어는 IndexedDB 파일 저장소와 전체 백업·복원·복제 흐름을 공유

v62.1 — 자유 메모 이미지 크기 조절
- 자유 메모 편집기에서 이미지를 누르면 선택 테두리와 조절 패널이 열립니다.
- 네 모서리를 드래그해 비율을 유지한 채 크기를 바꾸거나, 가로·세로 픽셀 수를 직접 입력할 수 있습니다.
- 비율 고정 해제 시 가로·세로를 독립적으로 조정할 수 있고, 자동 크기로 되돌릴 수 있습니다.
- 저장 데이터에는 이미지의 `width`·`height` 인라인 스타일만 기록되어 HTML 보기·백업·복원 뒤에도 크기가 유지됩니다.

v62 — 통합 인물 카드: 기존 페르소나를 데이터 손실 없이 단일 모드 캐릭터로 자동 승격 · 새 메모에서 페르소나(1인) / 캐릭터 모음(다인) 선택 · 페이지별 크리에이터 메모 통일 · 기존 캐릭터 메모는 컬렉션 모드 유지
- 첫 실행 시 변환 전 자동 백업을 남기며, 알려지지 않은 기존 필드는 `legacyPersonaExtras`에 보존합니다.
- 단일 페르소나는 메뉴에서 캐릭터 모음으로 확장할 수 있고, 페이지가 하나인 모음은 다시 단일 모드로 정리할 수 있습니다.

v61.1 — 캐릭터 메모 크리에이터 노트 팝업 닫기 오류 수정
- 존재하지 않는 `attachReadCodeCopy()` 호출을 `addCodeCopyButtons()`로 교정했습니다.
- 코드 복사 버튼 생성 실패와 관계없이 닫기 이벤트가 먼저 등록되도록 보강했습니다.

v61 — HTML 작업실: 원본 HTML 문자열 보존 · 샌드박스 미리보기 · 문서형/원본형 가져오기 분리

v59 상세 매뉴얼 내장 — 설정에서 전체 기능 매뉴얼 열기 · 데스크톱/모바일 닫기 · 기기 뒤로가기로 앱 복귀

v59 프로젝트 패키지 — 프로젝트·모든 메모·본문 이미지·자유 메모 첨부파일을 한 HTML로 내보내고, ID 충돌 없는 새 프로젝트로 불러오기

v59 로그 템플릿 소개/팝업 보정 — 소개 페이지 닫기 버튼 · 50종 가나다순 통합 · 모바일 템플릿 목록만 스크롤하고 하단 닫기 고정

v59 로그 외부 템플릿 47종 따옴표 보완 — 큰따옴표·작은따옴표 강조 규칙을 수정한 전체 세트로 교체 · 최초 기본 3종은 유지

v59 로그 외부 템플릿 47종 재배치 — 설명과 디자인을 개편한 전체 세트로 교체 · 최초 기본 3종은 유지

v59 메인 프로젝트 스크롤 연결 보정 — 프로젝트가 적거나 목록의 위·아래 경계에 닿으면 휠·스와이프가 메인 화면으로 자연스럽게 이어짐

v59 로그 템플릿 50종 — 추가 디자인 47종 기본 제공 · 템플릿 창을 열 때 기본 탭/빈 검색/이름 오름차순으로 초기화 · 사용자 템플릿과 즐겨찾기만 보존

v59 로그 템플릿 40종 — 추가 디자인 37종 기본 제공 · 기본/사용자/즐겨찾기 탭 · 길게 눌러 상단 고정 · 이름순 정렬·내용 검색 · 라이트 모드 테마 잉크색 UI

v59 로그 템플릿 30종 — 추가 디자인 27종 기본 제공 · 로그 구분 마크 테마 연동 · 복수 원본 이름 가림

v59 로그 메모 확장 — 정규식 디자인 템플릿, 페르소나명 가림, 사용자 JSON 템플릿, 인라인 HTML 복사·파일 내보내기

v59 보수본 — PWA 아이콘 연결, 원자적 백업 복원, 고아 데이터 복구, 캐시 격리·오프라인 응답, 화면 확대 접근성 보정

v59 — 메인 프로젝트·최근 메모 메뉴의 세 점 아이콘을 테마별 그라데이션 아이콘으로 개선

v58 — 라이트 테마 제목·로고 스타·검색바 색상 연동 보정

v57 — 메인 프로젝트 아카이브 투명 배경·테두리 전용 보정

v56 — 캐릭터 메모 대표 이미지 직접 업로드 지원

v55 — 크리에이터 노트 보기 정리 · 대표 썸네일 · 홈 프로젝트 스크롤 박스 · 글래시한 명암 문구 조정

v54 — 슬림 프레임 10종 + 투명 펀칭 프레임 컬러 추가

# Lumi Ink (루미잉크) · v53

AI 롤플레이 창작을 위한 **올인원 메모 · 자료 관리 PWA**.
프로젝트(폴더)별로 자유 메모 · 로어북 · 페르소나 · 캐릭터 메모를 정리하고, SillyTavern으로 바로 내보낼 수 있어요. 모든 데이터는 기기 안(IndexedDB)에만 저장되며 서버로 전송되지 않습니다.

---

> **v53 투명 글래스 프레임:** 프레임 색상 팔레트에 `투명 글래스`를 추가했습니다. 반투명 화이트 글래스 톤으로 렌더링되며, 썸네일 위에 은은한 유리 액자처럼 겹쳐집니다. 팔레트에서는 체커보드 스와치로 구분됩니다.
>
> **v52 둥근 썸네일 복원:** 외곽 프레임을 유지하면서 썸네일 이미지만 별도 미디어 레이어에서 둥글게 클리핑하도록 구조를 분리했습니다. 따라서 프레임은 바깥 몰딩처럼 남고, 이미지·기본 로고는 다시 각 위치의 원래 라운딩을 유지합니다.
>
> **v51 프레임 외곽 몰딩 보정:** 프레임을 썸네일 내부 장식이 아니라 원래 테두리를 감싸는 외곽 액자로 렌더링하도록 조정했습니다. 홈 카드, 사이드바, 프로젝트 선택창, 상세 헤더, 프레임 미리보기에서 같은 비율을 유지합니다.
>
> **v50 프레임 색상 수정:** 프레임 팔레트에서 선택한 색상을 실제 HEX 값으로 저장하도록 바로잡았습니다. v49에서 저장된 `gold`, `pastel-pink` 등의 이전 키값도 앱을 열 때 자동으로 복구합니다.

## ✨ 주요 기능

### 프로젝트 (폴더)
- 메모를 프로젝트 단위로 묶어 관리
- 폴더 아이콘 이미지, 색상 태그, **태그 칩**(쉼표 구분) 지정
- 상단 고정(최대 6개), 정렬(최신순 / 오래된순 / 이름순)

### 자유 메모
- 서식 보존 리치 에디터(굵게·기울임·밑줄·취소선, 색상/형광펜, 정렬, 글자 크기, 목록)
- 이미지 삽입, 파일 첨부, **코드 블록**, **하이퍼링크**
- 지우개 버튼으로 서식·코드블록·링크를 한 번에 제거
- 형광펜 버튼 길게 누르기로 하늘색 · 핑크색 · 연두색 · 연보라색 · 노란색 선택
- 읽기 화면 코드블록 복사 버튼, 본문 길게 눌러 전체 복사
- 툴바 기본 상태를 **항상 표시 / 접어두기**로 선택하고, 편집 중에는 즉시 접기·펼치기

### 로어북 (World Info)
- 마크다운 작성 + 보기 모드(제목 칩 박스, `- ` 삼각형 목록, `<태그></태그>` 박스, `- 라벨:` 미니 칩, `@@명령` 명령 칩)
- 발동 키워드, 항상 활성(constant), **삽입 깊이(Depth)** 설정
- 실시간 토큰 카운터(cl100k)
- **SillyTavern World Info JSON** 내보내기 / 불러오기(여러 개 묶음 지원)

### 페르소나 카드
- 포트레이트(3:4) · 정사각 썸네일 · 갤러리(멀티 업로드 + 드래그 정렬)
- 한국어 / 영어 이중 양식, 태그, 마크다운 상세
- 갤러리 확대 보기에서 이전 / 다음 화살표로 연속 탐색
- `<user name="{{user}}">` 류 제어 태그는 보기 화면에서 자동 숨김
- 토큰 카운터
- **HTML로 내보내기**(보기 양식 그대로) → 다시 "열기"로 페르소나 메모로 불러오기 가능(자료 이전/공유용)

### 캐릭터 메모
- 한 메모 안에 **여러 캐릭터 페이지**를 생성·관리
- 각 페이지별 포트레이트 · 정사각 썸네일 · 갤러리 · 한국어/영어 설정 · 태그 · 토큰 수 제공
- 상세 설명 아래에 비공개 성격의 **크리에이터 메모** 기록 — 자유 메모와 같은 리치 서식·이미지·링크·코드블록 편집 지원
- 상단 썸네일 네비게이션, 좌우 화살표, 모바일 스와이프로 캐릭터 페이지 전환
- 보기 모드에서 크리에이터 메모는 미리보기와 **크게 보기** 팝업 제공
- HTML 내보내기 시 크리에이터 메모를 **포함 / 제외** 선택, 다시 열기로 캐릭터 메모 재가져오기 지원

### 공통
- 검색(제목 / 내용 / 제목+내용), 다중 선택(이동·삭제), 상단 고정
- 라이트 / 다크 테마, 웹폰트 전환
- 수동 저장 버튼 + **Ctrl/⌘ + S** 단축키(자동 저장과 별개로 즉시 저장)
- 전체 백업(.html) / 복원, 데이터 초기화

---

## 📲 설치 방법

웹앱(PWA)이라 설치가 매우 간단합니다.

### Android (Chrome)
1. 배포된 주소를 Chrome으로 엽니다.
2. 우측 상단 ⋮ 메뉴 → **앱 설치**(또는 "홈 화면에 추가").
3. 홈 화면 아이콘으로 실행하면 전체화면 앱처럼 동작합니다.

### iOS (Safari)
1. Safari로 주소를 엽니다.
2. 공유 버튼 → **홈 화면에 추가**.

### 데스크톱 (Chrome / Edge)
1. 주소창 오른쪽의 **설치** 아이콘 클릭, 또는 메뉴 → 앱 설치.

### 설치 아이콘 고르기
1. 앱의 **설정 → 앱 설치 아이콘**에서 잉크 블루 · 골드 · 바이올렛 · 로즈 · 포레스트 · 파스텔 블루 · 파스텔 핑크 · 파스텔 그린 · 파스텔 퍼플 · 파스텔 옐로우 중 하나를 선택합니다.
2. 열리는 설치 전용 페이지에서 **이 아이콘으로 설치**를 누르거나 브라우저 메뉴의 앱 설치를 실행합니다.
3. 이미 설치된 앱의 아이콘을 바꾸려면 기존 설치본을 제거한 뒤 원하는 아이콘으로 다시 설치합니다.

> 오프라인에서도 동작합니다. 업데이트는 온라인 상태에서 앱을 다시 열면 자동 적용돼요(필요 시 새로고침).

---

## 🚀 직접 호스팅 (다운로드 후 사용)

정적 파일만으로 동작하므로 어디서든 호스팅할 수 있습니다.

1. `ink-memo-pwa` 폴더 전체를 받습니다.
2. 웹 서버에 올립니다. 예시(로컬 테스트):
   ```bash
   cd ink-memo-pwa
   python3 -m http.server 8080
   # 브라우저에서 http://localhost:8080
   ```
   GitHub Pages 같은 정적 호스팅의 하위 폴더에 그대로 올려도 됩니다(모든 경로가 상대경로).
3. 처음 접속하면 서비스워커가 캐시를 구성하고, 이후 오프라인에서도 열립니다.

> 모든 데이터는 브라우저(IndexedDB)에 저장됩니다. 기기/브라우저를 옮길 때는 **설정 → 전체 백업**으로 `.html` 파일을 만들어 새 기기에서 **복원**하세요.

---

## 🔗 내보내기 요약
| 종류 | 형식 | 용도 |
|---|---|---|
| 프로젝트 | 패키지 `.html` | 메모·이미지·첨부파일을 포함한 프로젝트 단위 공유 · 이전 · 재가져오기 |
| 로어북 | World Info `.json` | SillyTavern 가져오기 · 내보내기 |
| 페르소나 | `.html` | 보기 양식 그대로 보관 · 공유 · 재가져오기 |
| 캐릭터 메모 | `.html` | 다중 캐릭터 카드 보관 · 공유 · 재가져오기 |
| 자유 메모 | `.html` | 보관 · 공유 · 재가져오기 |
| 전체 | 백업 `.html` | 기기 이전 / 복원 |

---

*Lumi Ink는 로컬 우선(local-first) 도구입니다. 소중한 자료는 주기적으로 백업해 주세요.*


---

## v45 신뢰성 · UX 개편
- 자유 메모·로어북·페르소나 텍스트에 로컬 임시 초안 안전망 및 다음 실행 시 복구 선택
- `tokenizer.js` 오프라인 사전 캐싱
- 이미지 입력 제한: 파일 12MB 이하, 2,400만 픽셀 이하
- 설정에서 브라우저 저장공간, 첨부 용량, 자동 백업 개수 표시
- 페르소나 갤러리 확대 화면: 좌우 스와이프 및 인접 이미지 사전 로드
- 메모·프로젝트·갤러리 이미지 삭제 후 6초간 되돌리기
- 전체/자동 백업 복원: 병합 또는 완전 교체, 실행 전 현재 상태 자동 백업


---

## v46 캐릭터 · 설치 경험 개편
- PWA 표시 이름을 **루미잉크**로 통일
- 설치 전 아이콘 선택: 잉크 블루 · 바이올렛 · 로즈 · 포레스트
- 로어북 `@@명령` 전용 명령 칩 및 명령 행 강조
- 자유 메모 툴바 항상 표시 / 접어두기 기본 상태 설정
- 다중 페이지 **캐릭터 메모**와 HTML 내보내기·재가져오기


---

## v47 편집 안정성 · 캐릭터 메모 보강
- 자유 메모 편집 세션을 메모 ID별로 분리하고, 뒤늦은 blur·자동저장 타이머가 다른 빈 메모를 덮어쓰지 않도록 저장 순서를 직렬화
- 캐릭터 메모 네비게이션 바에 밝은 표면 배경과 경계감을 추가
- 캐릭터 이미지 업로드는 시스템 **파일 선택기**를 열도록 변경
- 크리에이터 메모를 리치 에디터로 개편하고, 보기 화면의 크게 보기 팝업 추가
- 설치 아이콘을 동일한 펜촉 실루엣으로 통일하고 골드 및 5종 파스텔 컬러 추가 (총 10종)
- 로어북 · 페르소나 · 캐릭터 상세 마크다운에서 `#제목`, `##제목`, `-내용`처럼 공백 없는 표기도 동일하게 렌더링


---

## v51 프레임 외곽 몰딩 보정
- 프레임을 썸네일의 원래 테두리 위와 바깥으로 확장해, 그림 내부 장식 대신 물리적인 액자처럼 보이도록 수정
- 홈 프로젝트 카드 · 사이드바 · 프로젝트 선택창 · 상세 헤더 · 프레임 미리보기의 렌더링 비율 통일
- 프레임 적용 시 원래 썸네일 테두리가 프레임과 겹쳐 보이지 않도록 보정


---

## v52 둥근 썸네일 복원
- 프레임을 위해 외곽 레이어를 열어 두되, 이미지·기본 로고 전용 `.thumb-media` 레이어에서 원래의 둥근 모서리를 다시 클리핑
- 홈 카드 · 사이드바 · 프로젝트 선택창 · 상세 헤더 · 프레임 미리보기 모두에 동일한 이중 레이어 적용
- 프레임은 썸네일 바깥 몰딩으로 유지하고, 내부 이미지만 부드러운 라운딩을 보존
v59 모바일 크게 모드 보정 — 로그 템플릿 팝업 높이에 앱 확대 배율 반영 · 로그 본문 제목에도 페르소나 이름 가림 적용


## v63.5 — 아이디어 보드 색상·캔버스 이미지

- 투명 PNG/GIF를 어두운 배경판 없이 스티커처럼 배치합니다.
- 포스트잇·음악·메모 링크·첨부파일은 앱의 20종 컬러 테마를 조각별로 고정 선택할 수 있습니다.
- 캔버스에는 20종 컬러의 빈 바탕과 업로드 이미지 배경(오버레이 컬러·투명도 조절)을 제공합니다.
- 캔버스 이미지 파일은 보드 첨부로 관리되어 백업·복원·복제·프로젝트 가져오기에서 함께 보존됩니다.
